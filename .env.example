# Postgres
POSTGRES_USER=ping
POSTGRES_PASSWORD=ping
POSTGRES_DB=ping

# Backend
JWT_SECRET=troque-este-segredo-em-producao
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440
CORS_ORIGINS=*

# Firebase (coloque o JSON de credenciais na raiz como firebase-credentials.json)
FIREBASE_CREDENTIALS_PATH=/app/firebase-credentials.json
